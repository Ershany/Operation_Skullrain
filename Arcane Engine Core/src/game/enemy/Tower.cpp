#include "Tower.h"

namespace arcane {
	namespace game {
		namespace enemy {

			/* Tree position is in terrain space (not world space) */

			Tower::Tower(graphics::Renderable3D *renderable) : Entity(renderable) {}

			void Tower::update(float deltaTime) {

			}

			void Tower::onRender() {

			}

		}
	}
}